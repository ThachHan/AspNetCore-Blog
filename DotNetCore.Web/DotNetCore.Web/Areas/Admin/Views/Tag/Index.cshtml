@using DotNetCore.Web.Areas.Admin.Models
@model List<DotNetCore.Web.Areas.Admin.Models.TagViewModel>
@{
    ViewBag.Title = "Tag";
}

<div class="col-xs-12 page-title">
    <div class="title_left">
        <h3>Advertisement</h3>
    </div>
    <div class="title_right">
        <div class="pull-right">
            @Html.ActionLink("New Tag", "Create", "Tag")
        </div>
    </div>
</div>

<div class="col-xs-12 main-content">
    @if (!String.IsNullOrEmpty(ViewBag.MessageAlert))
    {
        <div class="alert alert-success fade in">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <span>@ViewBag.MessageAlert</span>
        </div>
    }

    <table class="table table-striped table-dark table-hover">
        <thead class="thead-inverse">
            <tr>
                <th scope="row" class="width-5">
                    Id
                </th>
                <th scope="row" class="width-5">
                    Tag Name
                </th>            
                <th scope="row" class="width-3">Action</th>
            </tr>
        </thead>
        @if (Model != null && Model.Count > 0)
        {
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @item.TagId
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TagName)
                    </td>
                  
                    <td>
                        <a href="@Url.Action("Edit", new { id = item.TagId })" class="btn btn-default">
                            <i class="fa fa-pencil" aria-hidden="true"></i> Edit
                        </a>
                    </td>
                </tr>
                }
            </tbody>
        }
    </table>
</div>
